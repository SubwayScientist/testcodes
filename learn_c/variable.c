#include <stdio.h>
#include <stdlib.h>

int main() {

    char name[] = "John";
    int age = 35;
    printf("There once was a man named %s\n", name);
    printf("He was %d years old.\n", age);

    return 0;
}
